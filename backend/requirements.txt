# Core API
fastapi==0.128.0
uvicorn[standard]==0.35.0
sqlalchemy==2.0.30
# Required when DATABASE_URL points at Render Postgres. The binary extra keeps
# Docker builds self-contained instead of requiring a compiler and libpq.
psycopg[binary]>=3.2,<4
pydantic==2.11.7
pydantic-settings>=2.6.1

# Auth
python-jose[cryptography]==3.3.0
bcrypt>=4.0.1,<5.0.0
python-multipart>=0.0.9

# Misc
python-dotenv>=1.0.1
httpx>=0.28,<1
Pillow>=10.0.0

# --- Optional --------------------------------------------------------------
# Real inference for the Lumpy Skin Disease dashboard. Without this
# installed, that dashboard runs in demo mode automatically - nothing
# breaks. Install it once you have a trained best.pt to plug in
# (see LUMPY_MODEL_PATH in .env.example). Pulls in PyTorch, so it's a large
# download - that's why it isn't installed by default.
# ultralytics>=8.3.0
