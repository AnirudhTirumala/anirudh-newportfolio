import type { DashboardKey } from "@/types";

/** Cosmetic address-bar text for the project preview and live demo. */
export const DASHBOARD_URLS: Partial<Record<DashboardKey, string>> = {
  lumpy: "lumpydetect.ai/app",
  janseva: "janseva-connect.gov.in/portal",
};
